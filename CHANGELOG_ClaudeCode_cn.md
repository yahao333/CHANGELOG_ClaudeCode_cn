# 更新日志

## 2.1.92

- 新增 `forceRemoteSettingsRefresh` 策略设置：启用后，CLI 启动时会阻止直到远程托管设置被重新获取，如果获取失败则退出（fail-closed 模式）
- 新增交互式 Bedrock 设置向导，在选择"第三方平台"登录时从登录界面可访问——引导完成 AWS 认证、区域配置、凭证验证和模型固定
- 为订阅用户新增按模型和缓存命中分类的 `/cost` 展示
- `/release-notes` 现为交互式版本选择器
- 远程控制会话名称现使用主机名作为默认前缀（例如 `myhost-graceful-unicorn`），可通过 `--remote-control-session-name-prefix` 覆盖
- Pro 用户现可在提示缓存过期后返回会话时看到底部提示，显示下一轮将发送多少未缓存的 token
- 修复 tmux 窗口在长时间运行会话期间被杀死或重新编号后，子代理生成永久失败并报"Could not determine pane count"的问题
- 修复提示类型 Stop hooks 在小快速模型返回 `ok:false` 时错误失败的问题，并恢复非 Stop 提示类型 hooks 的 `preventContinuation:true` 语义
- 修复流式传输将数组/对象字段作为 JSON 编码字符串发出时的工具输入验证失败
- 修复扩展思考产生空白文本块伴随真实内容时可能出现的 API 400 错误
- 修复自动驾驶按键和连续提示数字碰撞导致的意外反馈调查提交
- 修复全屏模式处理期间文本选择存在时，误导性的"esc to interrupt"提示与"esc to clear"一同出现的问题
- 修复 Homebrew 安装更新提示使用 cask 的发布渠道（`claude-code` → stable，`claude-code@latest` → latest）
- 修复多行提示中已在下一行末尾时 `ctrl+e` 跳转到行尾的问题
- 修复在 iTerm2、Ghostty 及支持 DEC 2026 的其他终端中，全屏模式滚动时同一消息可能出现在两个位置的问题
- 修复空闲返回"/clear to save X tokens"提示显示累积会话 token 而非当前上下文大小的问题
- 修复会话启动时插件 MCP 服务器在重复未经认证的 claude.ai 连接器时卡在"connecting"状态
- 提升大文件 Write 工具 diff 计算速度（带 tab/`&`/`$` 的文件提升 60%）
- 移除 `/tag` 命令
- 移除 `/vim` 命令（通过 `/config` → Editor mode 切换 vim 模式）
- Linux 沙盒现同时在 npm 和原生构建中提供 `apply-seccomp` 帮助程序，恢复沙盒命令的 unix-socket 阻塞

## 2.1.91

- 新增通过 `_meta["anthropic/maxResultSizeChars"]` 注解覆盖 MCP 工具结果持久化（最高 500K），允许更大的结果（如数据库 schema）直接通过不被截断
- 新增 `disableSkillShellExecution` 设置以禁用技能、自定义斜杠命令和插件命令中的内联 shell 执行
- 新增对 `claude-cli://open?q=` 深链接中多行提示的支持（编码换行符 `%0A` 不再被拒绝）
- 插件现可将在 `bin/` 下打包可执行文件，并作为裸命令从 Bash 工具调用
- 修复 `--resume` 时转录链断裂可能导致异步转录写入失败时丢失对话历史
- 修复 iTerm2、kitty、WezTerm、Ghostty 和 Windows Terminal 上 `cmd+delete` 不删除到行首
- 修复远程会话中计划模式在容器重启后丢失计划文件跟踪，导致计划编辑时出现权限提示和空白计划审批弹窗
- 修复设置中 `permissions.defaultMode: "auto"` 的 JSON schema 验证
- 修复 Windows 版本清理不保护活动版本回滚副本
- `/feedback` 现解释为何不可用而非从斜杠菜单中消失
- 改进 `/claude-api` 技能指导，包含代理设计模式、上下文管理和缓存策略的工具面决策
- 提升性能：通过 `Bun.stripANSI` 路由使 Bun 上 `stripAnsi` 更快
- Edit 工具现使用更短的 `old_string` 锚点，减少输出 token

## 2.1.90

- 新增 `/powerup` — 交互式课程，通过动画演示教授 Claude Code 功能
- 新增 `CLAUDE_CODE_PLUGIN_KEEP_MARKETPLACE_ON_FAILURE` 环境变量，在 `git pull` 失败时保留现有 marketplace 缓存，适用于离线环境
- 新增 `.husky` 到受保护目录（acceptedits 模式）
- 修复达到使用限制后速率限制选项对话框无限循环自动打开最终导致会话崩溃的问题
- 修复 `--resume` 对具有延迟工具、MCP 服务器或自定义代理的用户导致首次请求完全缓存未命中（自 v2.1.69 以来的回归）
- 修复 PostToolUse format-on-save hook 在连续编辑之间重写文件时，Edit/`Write` 失败并显示"File content has changed"
- 修复向 stdout 发出 JSON 并以代码 2 退出的 PreToolUse hooks 未正确阻止工具调用
- 修复 CLAUDE.md 文件在工具调用期间自动加载时，折叠搜索/读取摘要徽章在全屏回滚中多次出现
- 修复自动模式不尊重明确的用户边界（"don't push"、"wait for X before Y"），即使该操作原本被允许
- 修复浅色终端主题上点击展开悬停文本几乎不可见
- 修复恶意工具输入到达权限对话框时的 UI 崩溃
- 修复 `/model`、`/config` 和其他选择屏幕滚动时标题消失
- 加强 PowerShell 工具权限检查：修复尾随 `&` 后台作业绕过、`-ErrorAction Break` 调试器挂起、存档提取 TOCTOU 和解析失败回退规则降级
- 提升性能：消除缓存键查找时每轮 JSON.stringify MCP 工具 schema
- 提升性能：SSE 传输现以线性时间处理大流式帧（原本是二次方）
- 提升性能：具有长对话的 SDK 会话在转录写入时不再二次方减速
- 改进 `/resume` 全项目视图以并行加载项目会话，改善具有众多项目的用户的加载时间
- 更改 `--resume` 选择器不再显示由 `claude -p` 或 SDK 调用创建的会话
- 移除 `Get-DnsClientCache` 和 `ipconfig /displaydns` 自动允许（DNS 缓存隐私）

## 2.1.89

- 新增 `"defer"` 权限决策到 `PreToolUse` hooks — 无头会话可在工具调用处暂停，通过 `-p --resume` 恢复让 hook 重新评估
- 新增 `CLAUDE_CODE_NO_FLICKER=1` 环境变量以选择无闪烁 alt-screen 渲染和虚拟化回滚
- 新增 `PermissionDenied` hook，在自动模式分类器拒绝后触发 — 返回 `{retry: true}` 告知模型可以重试
- 新增命名子代理到 `@` 提及类型建议
- 新增 `MCP_CONNECTION_NONBLOCKING=true` 用于 `-p` 模式完全跳过 MCP 连接等待，并将 `--mcp-config` 服务器连接限制为 5s 而非阻塞最慢的服务器
- 自动模式：被拒绝的命令现显示通知并出现在 `/permissions` → Recent 标签页，可在此用 `r` 重试
- 修复 `Edit(//path/**)` 和 `Read(//path/**)` 允许规则检查解析后的符号链接目标，而非仅检查请求路径
- 修复语音按键通话未为某些修饰符组合绑定激活，以及语音模式在 Windows 上因"WebSocket upgrade rejected with HTTP 101"失败
- 修复 Edit/Write 工具在 Windows 上双写 CRLF 并剥离 Markdown 硬换行（两个尾随空格）
- 修复 `StructuredOutput` schema 缓存错误导致使用多个 schema 时约 50% 失败率
- 修复大 JSON 输入作为 LRU 缓存键在长时间会话中保留的内存泄漏
- 修复在非常大的会话文件（超过 50MB）中删除消息时的崩溃
- 修复 LSP 服务器僵住状态后崩溃 — 服务器现改为在下次请求时重启而非会话重启前一直失败
- 修复提示历史条目在 `~/.claude/history.jsonl` 中落在 4KB 边界时包含 CJK 或 emoji 的条目被静默丢弃
- 修复 `/stats` 因排除子代理使用而少计 token，以及在统计缓存格式更改时丢失 30 天以上的历史数据
- 修复延迟工具输入超过 64KB 或不存在延迟标记时 `-p --resume` 挂起，以及 `-p --continue` 不恢复延迟工具
- 修复 `claude-cli://` 深链接在 macOS 上无法打开
- 修复 MCP 工具错误在服务器返回多元素错误内容时仅截断到第一个内容块
- 修复通过 SDK 发送图片时丢弃技能提醒和其他系统上下文
- 修复 PreToolUse/PostToolUse hooks 将 `file_path` 作为绝对路径接收，用于 Write/Edit/Read 工具，与记录的行为一致
- 修复自动压缩抖动循环 — 现检测上下文在压缩三次后立即回填到限制的情况并停止，改为显示可操作的错误而非燃烧 API 调用
- 修复提示缓存长时间会话中的缓存未命中，原因是工具 schema 字节在会话中发生变化
- 修复嵌套 CLAUDE.md 文件在读取许多文件的长时间会话中被重新注入数十次
- 修复转录包含来自较旧 CLI 版本或中断写入的工具结果时 `--resume` 崩溃
- 修复当 API 返回权限错误时误导性的"Rate limit reached"消息 — 现显示实际错误和可操作提示
- 修复 hooks `if` 条件过滤不匹配复合命令（`ls && git push`）或带环境变量前缀的命令（`FOO=bar git push`）
- 修复折叠搜索/读取组徽章在重型并行工具使用期间在终端回滚中重复
- 修复通知 `invalidates` 不立即清除当前显示的通知
- 修复在后端消息到达处理期间提交后提示短暂消失
- 修复天城文等组合标记文本在助手输出中被截断
- 修复布局更改后主屏幕终端上的渲染伪影
- 修复语音模式在 macOS Apple Silicon 上无法请求麦克风权限
- 修复 Shift+Enter 在 Windows Terminal Preview 1.25 上提交而非插入换行
- 修复 iTerm2 在 tmux 内运行时流式传输期间的周期性 UI 抖动
- 修复 PowerShell 工具在 Windows PowerShell 5.1 上错误报告 `git push` 等命令写入 stderr 的失败
- 修复 Edit 工具用于非常大文件（>1 GiB）时的潜在内存不足崩溃
- 改进折叠工具摘要，为 `ls`/`tree`/`du` 显示"Listed N directories"而非"Read N files"
- 改进 Bash 工具在格式化程序/林挺命令修改您之前读取的文件时发出警告，防止过时编辑错误
- 改进 `@` 提及类型建议对相似名称的源文件排名高于 MCP 资源
- 改进 PowerShell 工具提示，显示版本适当的语法指导（5.1 vs 7+）
- 更改 Edit 适用于通过 `Bash` 的 `sed -n` 或 `cat` 查看的文件，无需先单独调用 Read
- 更改 hook 输出超过 50K 字符时保存到磁盘并显示文件路径 + 预览，而非直接注入上下文
- 更改设置中 `cleanupPeriodDays: 0` 现以验证错误拒绝 — 之前会静默禁用转录持久化
- 更改思考摘要默认不再在交互会话中生成 — 在设置中设置 `showThinkingSummaries: true` 以恢复
- 记录 `TaskCreated` hook 事件及其阻塞行为
- 使用 Ctrl+B 后台运行命令时保留任务通知
- Windows 上的 PowerShell 工具：包含双引号和空白同时存在的外部命令参数现触发提示而非自动允许（PS 5.1 参数拆分加固）
- `/env` 现适用于 PowerShell 工具命令（之前仅影响 Bash）
- `/usage` 现为 Pro 和 Enterprise 计划隐藏冗余的"Current week (Sonnet only)"栏
- 图片粘贴不再插入尾随空格
- 将 `!command` 粘贴到空提示现进入 bash 模式，与键入 `!` 行为匹配
- `/buddy` 为 4 月 1 日而来 — 孵化一只观看您编码的小生物

## 2.1.87

- 修复 Cowork Dispatch 中消息未送达

## 2.1.86

- 新增 `X-Claude-Code-Session-Id` 头到 API 请求，使代理可以按会话聚合请求而无需解析 body
- 新增 `.jj` 和 `.sl` 到 VCS 目录排除列表，使 Grep 和文件自动完成不会进入 Jujutsu 或 Sapling 元数据
- 修复在 v2.1.85 之前创建的会话上 `--resume` 失败并显示"tool_use ids were found without tool_result blocks"
- 修复在项目根目录外（例如 `~/.claude/CLAUDE.md`）的 Write/Edit/Read 失败，当配置了条件技能或规则时
- 修复每次技能调用时不必要的配置磁盘写入，可能导致性能问题和 Windows 上配置损坏
- 修复在非常长会话上使用 `/feedback` 时潜在的内存不足崩溃，具有大型转录文件
- 修复 `--bare` 模式在交互会话中丢弃 MCP 工具并静默丢弃中轮排队的消息
- 修复 `c` 快捷方式仅复制 OAuth 登录 URL 约 20 个字符而非完整 URL
- 修复在窄终端多行环绕时屏蔽输入（例如 OAuth 代码粘贴）泄漏令牌开头
- 修复自 v2.1.83 以来官方 marketplace 插件脚本在 macOS/Linux 上失败并显示"Permission denied"
- 修复运行多个 Claude Code 实例并在其中之一使用 `/model` 时状态线显示另一个会话的模型
- 修复在长对话底部轮滚动或点击选择后滚动未跟随新消息
- 修复 `/plugin` 卸载对话框：按 `n` 现正确卸载插件同时保留其数据目录
- 修复按下点击后按 Enter 回归导致转录空白直到响应到达
- 修复删除关键词后 `ultrathink` 提示仍然存在
- 修复会话中 markdown/高亮渲染缓存保留完整内容字符串导致的内存增长
- 减少启动事件循环阻塞，当配置了许多 claude.ai MCP 连接器时（macOS 密钥链缓存从 5s 扩展到 30s）
- 减少使用 `@` 提及文件时的 token 开销 — 原始字符串内容不再进行 JSON 转义
- 通过从工具描述中移除动态内容，提升 Bedrock、Vertex 和 Foundry 用户的提示缓存命中率
- "Saved N memories"通知中的内存文件名现悬停高亮并点击打开
- `/skills` 列表中的技能描述现限制为 250 个字符以减少上下文使用
- 更改 `/skills` 菜单按字母顺序排序以便更容易扫描
- 自动模式现显示"unavailable for your plan"当被计划限制禁用时（之前是"temporarily unavailable"）
- [VSCode] 修复扩展在长时间运行操作期间错误显示"Not responding"
- [VSCode] 修复 OAuth 令牌刷新后（登录后 8 小时）将 Max 计划用户默认设置为 Sonnet
- Read 工具现使用紧凑行号格式并去重未更改的重新读取，减少 token 使用

## 2.1.85

- 新增 `CLAUDE_CODE_MCP_SERVER_NAME` 和 `CLAUDE_CODE_MCP_SERVER_URL` 环境变量到 MCP `headersHelper` 脚本，允许一个帮助程序为多个服务器服务
- 新增 hooks 的条件 `if` 字段，使用权限规则语法（例如 `Bash(git *)`）过滤运行时间，减少进程生成开销
- 新增调度任务（`/loop`、`CronCreate`）触发时在转录中的时间戳标记
- 新增粘贴图片时 `[Image #N]` 占位符后的尾随空格
- 深链接查询（`claude-cli://open?q=…`）现支持最多 5,000 个字符，长预填充提示带有"滚动查看"警告
- MCP OAuth 现遵循 RFC 9728 Protected Resource Metadata 发现以找到授权服务器
- 被组织策略（`managed-settings.json`）阻止的插件不能再安装或启用，并从 marketplace 视图中隐藏
- PreToolUse hooks 现可通过返回 `updatedInput` 以及 `permissionDecision: "allow"` 来满足 `AskUserQuestion`，实现通过自身 UI 收集答案的无头集成
- OpenTelemetry tool_result 事件中的 `tool_parameters` 现通过 `OTEL_LOG_TOOL_DETAILS=1` 门控
- 修复 `/compact` 在对话增长过大以至于压缩请求本身无法容纳时失败并显示"context exceeded"
- 修复 `/plugin enable` 和 `/plugin disable` 在插件安装位置与设置中声明位置不同时失败
- 修复 `--worktree` 在 `WorktreeCreate` hook 运行前在非 git 仓库中以错误退出
- 修复 `deniedMcpServers` 设置未阻止 claude.ai MCP 服务器
- 修复计算机使用工具中 `switch_display` 在多显示器设置上返回"not available in this session"
- 修复当 `OTEL_LOGS_EXPORTER`、`OTEL_METRICS_EXPORTER` 或 `OTEL_TRACES_EXPORTER` 设置为 `none` 时的崩溃
- 修复差异语法高亮在非原生构建中不工作
- 修复在存在刷新令牌时 MCP 步进授权失败 — 通过 `403 insufficient_scope` 请求提升作用域的服务器现正确触发重新授权流程
- 修复流式响应中断时远程会话中的内存泄漏
- 修复在边缘连接搅动期间持续 ECONNRESET 错误，通过在重试时使用新的 TCP 连接
- 修复某些斜杠命令运行后提示卡在队列中，上箭头无法检索
- 修复 Python Agent SDK：通过 `--mcp-config` 传递的 `type:'sdk'` MCP 服务器不再在启动期间丢弃
- 修复通过 SSH 或 VS Code 集成终端运行时原始密钥序列出现在提示中
- 修复权限解析后远程控制会话状态保持卡在"Requires Action"
- 修复 shift+enter 和 meta+enter 被类型建议拦截而非插入换行
- 修复流式传输时滚动到上方的陈旧内容渗透
- 修复在 Ghostty、Kitty、WezTerm 和其他支持 Kitty 键盘协议的终端中退出后终端处于增强键盘模式 — Ctrl+C 和 Ctrl+D 退出后现正确工作
- 改进 @-提及文件自动完成性能在大仓库上
- 改进 PowerShell 危险命令检测
- 改进大转录滚动性能，用纯 TypeScript 实现替换 WASM yoga-layout
- 减少压缩触发大型会话时的 UI 抖动

## 2.1.84

- 为 Windows 新增 PowerShell 工具作为选填预览。在 https://code.claude.com/docs/en/tools-reference#powershell-tool 了解更多
- 新增 `ANTHROPIC_DEFAULT_{OPUS,SONNET,HAIKU}_MODEL_SUPPORTS` 环境变量以覆盖 3p（Bedrock、Vertex、Foundry）固定默认模型的 effort/思考能力检测，以及 `_MODEL_NAME`/`_DESCRIPTION` 用于自定义 `/model` 选择器标签
- 新增 `CLAUDE_STREAM_IDLE_TIMEOUT_MS` 环境变量以配置流式传输空闲看门狗阈值（默认 90s）
- 新增 `TaskCreated` hook，在通过 `TaskCreate` 创建任务时触发
- 新增 `WorktreeCreate` hook 对 `type: "http"` 的支持 — 通过响应 JSON 中的 `hookSpecificOutput.worktreePath` 返回创建的工作树路径
- 新增 `allowedChannelPlugins` 托管设置，供团队/企业管理员定义通道插件允许列表
- 新增 `x-client-request-id` 头到 API 请求用于调试超时
- 新增空闲返回提示，在 75+ 分钟后返回的用户中推动使用 `/clear`，减少陈旧会话上不必要的 token 重新缓存
- 深链接（`claude-cli://`）现于您的首选终端打开，而非检测列表中碰巧第一个的终端
- 规则和技能 `paths:` 前置元数据现接受 YAML glob 列表
- MCP 工具描述和服务器指令现限制为 2KB 以防止 OpenAPI 生成的服务器膨胀上下文
- 同时在本地和通过 claude.ai 连接器配置的 MCP 服务器现去重 — 本地配置优先
- 出现在交互提示上卡住的后台 bash 任务现约 45 秒后显示通知
- ≥1M 的 token 计数现显示为"1.5m"而非"1512.6k"
- 当 `ToolSearch` 启用时全局系统提示缓存现工作，包括配置了 MCP 工具的用户
- 修复语音按键通话：按住语音键不再泄漏字符到文本输入，转录现正确位置插入
- 修复当底部项目获得焦点时上/下箭头键无响应
- 修复 `Ctrl+U`（kill-to-line-start）在多行输入行边界处无效，因此重复 `Ctrl+U` 现跨行清除
- 修复取消绑定默认 chord 绑定（例如 `"ctrl+x ctrl+k": null`）仍进入 chord-wait 模式而非释放前缀键
- 修复鼠标事件在转录搜索输入中插入字面"mouse"文本
- 修复外部会话使用 `--json-schema` 且子代理也指定 schema 时工作流子代理失败并显示 API 400
- 修复某些终端上用户消息气泡中某些 emoji 后面的背景色缺失
- 修复"允许 Claude 为此会话编辑其自身设置"权限选项对具有 `Edit(.claude)` 允许规则的用户不生效
- 修复为大编辑文件生成附件片段时的挂起
- 修复服务器重新连接时 MCP 工具/资源缓存泄漏
- 修复部分克隆仓库（Scalar/GVFS）触发大量 blob 下载时的启动性能问题
- 修复本机终端光标未跟踪文本输入插入符号，因此 IME 组合（CJK 输入）现内联渲染，屏幕阅读器可以跟随输入位置
- 修复 macOS 上瞬态密钥链读取失败导致的虚假"Not logged in"错误
- 修复冷启动竞争，其中核心工具可能被延迟而没有其绕过激活，导致 Edit/Write 在类型参数上失败并显示 InputValidationError
- 改进对危险移除 Windows 驱动器根（`C:\`、`C:\Windows` 等）的检测
- 改进交互式启动约 30ms，通过并行运行 `setup()`
- 改进 `claude "prompt"` 与 MCP 服务器的启动 — REPL 现立即渲染而非阻塞直到所有服务器连接
- 改进远程控制以在被阻止时显示具体原因而非通用"not yet enabled"消息
- 改进 p90 提示缓存命中率
- 减少长会话中滚动到顶部重置，通过使消息窗口对压缩和分组变化免疫
- 减少动画工具进度滚动到视口上方时的终端闪烁
- 更改问题/PR 引用仅在写成 `owner/repo#123` 时变为可点击链接 — 裸 `#123` 不再自动链接
- 斜杠命令对当前 auth 设置不可用（`/voice`、`/mobile`、`/chrome`、`/upgrade` 等）现隐藏而非显示
- [VSCode] 新增速率限制警告横幅，显示使用百分比和重置时间
- /stats 中截图（Ctrl+S）现适用于所有构建且速度快 16 倍

## 2.1.83

- 新增 `managed-settings.d/` 目录与 `managed-settings.json` 并列，让不同团队部署按字母顺序合并的独立策略片段
- 新增 `CwdChanged` 和 `FileChanged` hook 事件用于反应式环境管理（例如 direnv）
- 新增 `sandbox.failIfUnavailable` 设置，在启用沙盒但无法启动时退出并报错，而非无沙盒运行
- 新增 `disableDeepLinkRegistration` 设置以防止 `claude-cli://` 协议处理器注册
- 新增 `CLAUDE_CODE_SUBPROCESS_ENV_SCRUB=1` 以从子进程环境（Bash 工具、hooks、MCP stdio 服务器）中剥离 Anthropic 和云提供商凭证
- 新增转录搜索 — 在转录模式（`Ctrl+O`）中按 `/` 搜索，`n`/`N` 逐步浏览匹配
- 新增 `Ctrl+X Ctrl+E` 作为打开外部编辑器的别名（readline 原生绑定；`Ctrl+G` 仍然有效）
- 粘贴的图片现将 `[Image #N]` 芯片插入光标处，以便您可以在提示中按位置引用
- 代理现可在前置元数据中声明 `initialPrompt` 以自动提交第一轮
- `chat:killAgents` 和 `chat:fastMode` 现可通过 `~/.claude/keybindings.json` 重新绑定
- 修复鼠标跟踪转义序列在退出后泄漏到 shell 提示
- 修复 Claude Code 在 macOS 上退出时挂起
- 修复空闲几秒后屏幕闪烁空白
- 修复在共同行很少的大文件进行 diff 时挂起 — diff 现 5 秒后超时并优雅降级
- 修复在启用语音输入时启动时 1-8 秒 UI 冻结，由急切加载本机音频模块导致
- 修复启动回归，其中 Claude Code 在继续之前等待约 3 秒 claude.ai MCP 配置获取
- 修复 `--mcp-config` CLI 标志绕过 `allowedMcpServers`/`deniedMcpServers` 托管策略执行
- 修复 claude.ai MCP 连接器（Slack、Gmail 等）在单轮 `--print` 模式中不可用
- 修复 `caffeinate` 进程在 Claude Code 退出时未正确终止，防止 Mac 入睡
- 修复 tab 接受 `!` 前缀命令建议时 bash 模式未激活
- 修复在导航建议后陈旧斜杠命令选择显示错误的高亮命令
- 修复 `/config` 菜单同时显示搜索光标和列表选择
- 修复上下文压缩后后台子代理变得不可见，可能导致重复代理生成
- 修复后台代理任务在 git 或 API 调用期间清理挂起时保持"running"状态
- 修复 `--channels` 在升级后首次启动时显示"Channels are not currently available"
- 修复卸载的插件 hooks 继续触发直到下次会话
- 修复流式传输响应期间排队的命令闪烁
- 修复斜杠命令在消息处理期间提交时被作为文本发送给模型
- 修复在滚动到屏幕外完成后折叠读取/搜索组时回滚跳跃
- 修复在模型开始或停止思考时回滚跳转到顶部
- 修复由于 hooks 进度/附件消息分叉 parentUuid 链导致的 SDK 会话历史恢复丢失
- 修复在终端窗口外释放鼠标时 copy-on-select 未触发
- 修复高度受限列表中项目溢出时出现幽灵字符
- 修复 `Ctrl+B` 干扰空闲提示处的 readline backward-char — 现仅在可以后台前台任务时触发
- 修复工具结果文件从未被清理，忽略 `cleanupPeriodDays` 设置
- 修复释放语音按住说话后空格键被吞没最多 3 秒
- 修复在没有音频硬件的 Linux 上使用语音模式时 ALSA 库错误破坏终端 UI（Docker、无头、WSL1）
- 修复 Termux/Android 上语音模式 SoX 检测，其中生成 `which` 受内核限制
- 修复远程控制会话在 web 会话列表中积极运行时显示为空闲
- 修复在配置驱动模式中底部导航选择不可见的远程控制药丸
- 修复远程会话中工具使用 ID 无限累积的内存泄漏
- 改进 Bedrock SDK 冷启动延迟，通过重叠配置文件获取与其他启动工作
- 改进大会话上 `--resume` 的内存使用和启动延迟
- 改进插件启动 — 命令、技能和代理现从磁盘缓存加载，无需重新获取
- 改进远程控制会话标题：AI 生成的标题现出现在第一条消息后几秒内
- 改进 `WebFetch` 标识为 `Claude-User`，以便站点运营商可以通过 `robots.txt` 识别并允许 Claude Code 流量
- 减少大页面 `WebFetch` 峰值内存使用
- 减少长会话中回滚重置，从每轮一次减少到约每 50 条消息一次
- 具有未经身份验证的 HTTP/SSE MCP 服务器时 `claude -p` 启动更快（节省约 600ms）
- Bash ghost-text 建议现立即包含刚提交的命令
- 增加非流式回退 token 上限（21k → 64k）和超时（120s → 300s 本地），使回退请求不太可能被截断
- 在任何响应之前中断提示现自动恢复您的输入，以便您可以编辑并重新提交
- `/status` 现可在 Claude 响应时工作，而非排队直到轮次完成
- 重复 org 管理连接器的插件 MCP 服务器现被抑制，而非运行第二个连接
- Linux：在注册 `claude-cli://` 协议处理器时尊重 `XDG_DATA_HOME`
- 更改"stop all background agents"快捷绑定从 `Ctrl+F` 到 `Ctrl+X Ctrl+K`，以停止掩盖 readline forward-char
- 弃用 `TaskOutput` 工具，转而使用 `Read` 读取后台任务的输出文件路径
- 新增 `CLAUDE_CODE_DISABLE_NONSTREAMING_FALLBACK` 环境变量以在流式传输失败时禁用非流式回退
- 插件选项（`manifest.userConfig`）现外部可用 — 插件可在启用时提示配置，`sensitive: true` 值存储在密钥链（macOS）或其他平台上的受保护凭证文件
- Claude 现可以引用剪贴板粘贴图片的磁盘路径用于文件操作
- `Ctrl+L` 现清除屏幕并强制完整重绘 — 在 Cmd+K 使 UI 部分空白时使用此恢复。使用 `Ctrl+U` 或双 Esc 清除提示输入。
- `--bare -p`（SDK 模式）到 API 请求快约 14%
- 内存：`MEMORY.md` 索引现也在 25KB 处截断以及 200 行
- 在 `--channels` 激活时禁用 `AskUserQuestion` 和计划模式工具
- 修复粘贴图片在失败工具调用期间排队时的 API 400 错误
- 修复 SSE 连接在调用中间断开并耗尽其重新连接尝试时 MCP 工具调用无限期挂起
- 修复在后台代理在第一条用户消息之前完成时远程控制会话标题显示原始 XML
- 修复由于恢复的转录链中进度消息间隙，远程会话在容器重启后忘记对话历史
- 修复在瞬态 auth 错误时远程会话需要重新登录而非自动重试
- 修复在 Linux 沙盒模式下 `rg ... | wc -l` 及类似管道命令挂起并返回 `0`
- 修复在 CJK IME 插入全角空格时语音输入按住说话未激活
- 修复 `--worktree` 在工作树名称包含正斜杠时静默挂起
- [VSCode] 后端 60 秒未响应时 spinner 变为红色并显示"Not responding"
- [VSCode] 修复通过 URL 重新打开会话或重启后会话历史未正确加载
- [VSCode] 新增 Esc 两次（或 `/rewind`）打开键盘可导航的回退选择器
- [VSCode] 修复"从这里 fork 对话"和回退操作在会话缓存变陈后静默失败

## 2.1.81

- 为脚本化 `-p` 调用新增 `--bare` 标志 — 跳过 hooks、LSP、插件同步和技能目录遍历；需要 `ANTHROPIC_API_KEY` 或通过 `--settings` 的 `apiKeyHelper`（禁用 OAuth 和密钥链 auth）；完全禁用自动内存
- 新增 `--channels` 权限中继 — 声明权限能力的通道服务器可以将工具批准提示转发到您的手机
- 修复多个并发 Claude Code 会话在一个会话刷新其 OAuth 令牌时需要重复重新认证
- 修复语音模式静默吞下重试失败并显示误导性的"check your network"而非实际错误
- 修复语音模式音频在服务器静默断开 WebSocket 连接时未恢复
- 修复 `CLAUDE_CODE_DISABLE_EXPERIMENTAL_BETAS` 未抑制结构化输出 beta 头，导致转发到 Vertex/Bedrock 的代理网关出现 400 错误
- 修复没有其他托管设置配置的团队/企业组织的 `--channels` 绕过
- 修复 Node.js 18 上崩溃
- 修复 Bash 命令中字符串包含破折号时不必要的权限提示
- 修复插件目录在会话中间删除时插件 hooks 阻止提示提交
- 修复后台代理任务输出在任务在轮询间隔之间完成时可以无限期挂起的竞争条件
- 恢复处于工作树中的会话现在切换回该工作树
- 修复 `/btw` 在活动响应期间使用时未包含粘贴文本
- 修复快速 Cmd+Tab 后跟粘贴可能击败 tmux 下的剪贴板复制的竞争条件
- 修复终端标签标题未使用自动生成的会话描述更新
- 修复不可见 hook 附件在转录模式中膨胀消息计数
- 修复远程控制会话显示通用标题而非从第一条提示派生
- 修复 `/rename` 未同步远程控制会话的标题
- 修复远程控制 `/exit` 未可靠归档会话
- 改进 MCP 读取/搜索工具调用折叠为单个"Queried {server}"行（用 Ctrl+O 展开）
- 改进 `!` bash 模式可发现性 — Claude 现在线需要运行交互命令时建议它
- 改进插件新鲜度 — 带有跟踪的插件现每次加载时重新克隆以获取上游更改
- 改进远程控制会话标题在第三条消息后刷新
- 更新 MCP OAuth 以支持客户端 ID 元数据文档（CIMD / SEP-991）用于没有动态客户端注册的服务器
- 更改计划模式默认隐藏"clear context"选项（用 `"showClearContextOnPlanAccept": true` 恢复）
- 在 Windows（包括 Windows Terminal 中的 WSL）上禁用逐行响应流式传输，由于渲染问题
- [VSCode] 修复使用 Git Bash 时 Windows PATH 继承 Bash 工具（v2.1.78 回归）

## 2.1.80

- 新增 `rate_limits` 字段到状态线脚本用于显示 Claude.ai 速率限制使用（5 小时和 7 天窗口，含 `used_percentage` 和 `resets_at`）
- 新增 `source: 'settings'` 插件 marketplace 来源 — 在 settings.json 中内联声明插件条目
- 新增 CLI 工具使用检测到插件提示，除了文件模式匹配
- 新增对技能和斜杠命令的 `effort` 前置元数据支持，以在调用时覆盖模型 effort 级别
- 新增 `--channels`（研究预览）— 允许 MCP 服务器将消息推入您的会话
- 修复 `--resume` 丢弃并行工具结果 — 具有并行工具调用的会话现恢复所有 tool_use/tool_result 对，而非显示 `[Tool result missing]` 占位符
- 修复由非浏览器 TLS 指纹上的 Cloudflare 机器人检测导致的语音模式 WebSocket 失败
- 修复通过 API 代理、Bedrock 或 Vertex 使用细粒度工具流式传输时的 400 错误
- 修复 `/remote-control` 在无法工作的网关和第三方提供商部署上显示
- 修复 `/sandbox` 标签切换不响应 Tab 或箭头键
- 改进大型 git 仓库中 `@` 文件自动完成的响应能力
- 改进 `/effort` 显示自动当前解析为什么，匹配状态栏指示器
- 改进 `/permissions` — Tab 和箭头键现可从列表内部切换标签
- 改进后台任务面板 — 左箭头现从列表视图关闭
- 简化插件安装提示，使用单一 `/plugin install` 命令而非两步流程
- 减少大仓库启动内存使用（在 250k 文件仓库上节省约 80 MB）
- 修复托管设置（`enabledPlugins`、`permissions.defaultMode`、策略设置环境变量）在 `remote-settings.json` 从先前会话缓存时未在启动时应用

## 2.1.79

- 为 `claude auth login` 新增 `--console` 标志用于 Anthropic Console（API 计费）认证
- 新增"显示轮次时长"切换到 `/config` 菜单
- 修复 `claude -p` 在作为子进程生成而没有明确 stdin 时挂起（例如 Python `subprocess.run`）
- 修复 Ctrl+C 在 `-p`（打印）模式不工作
- 修复 `/btw` 在流式传输期间触发时返回主代理输出而非回答附带问题
- 修复在设置中 `voiceEnabled: true` 时语音模式未在启动时正确激活
- 修复 `/permissions` 中左/右箭头标签导航
- 修复 `CLAUDE_CODE_DISABLE_TERMINAL_TITLE` 未在启动时阻止终端标题设置
- 修复工作区信任阻止时自定义状态行显示为空
- 修复企业用户无法在速率限制（429）错误时重试
- 修复使用交互式 `/resume` 切换会话时 `SessionEnd` hooks 未触发
- 改进启动内存使用约 18MB，应用于所有场景
- 改进非流式 API 回退，2 分钟每次尝试超时，防止会话无限期挂起
- `CLAUDE_CODE_PLUGIN_SEED_DIR` 现支持多个种子目录，以